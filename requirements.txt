# Core
streamlit==1.38.0
streamlit-autorefresh==1.0.1
requests==2.32.3
python-dateutil==2.9.0.post0
pytz==2024.1

# ABI-compatible pair (works well on Cloud)
numpy==2.0.2
pandas==2.2.2

# WebSocket client (we use EODHD endpoints directly)
websocket-client==1.8.0
